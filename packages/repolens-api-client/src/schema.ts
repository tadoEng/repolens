/**
 * GENERATED FILE — DO NOT EDIT BY HAND.
 *
 * Produced by `openapi-typescript` from the OpenAPI document that the Axum service emits
 * via utoipa. Regenerate with:
 *
 *     pnpm --filter @repolens/api-client schema:update
 *
 * `schema.test.ts` is the staleness gate: it regenerates this file from the committed
 * OpenAPI document and fails if the result differs. A hand edit here is therefore not a
 * shortcut — it is a build break waiting for the next CI run, and it silently decouples
 * the frontend's idea of the API from the backend's.
 */

/**
 * PLACEHOLDER — no OpenAPI document exists yet.
 *
 * The shape below is what `openapi-typescript` emits for an empty document, so the real
 * output drops in without touching a single import. The real schema arrives with the
 * walking skeleton's SystemProbeResponse (issue #11) and the analysis-v1 / report-v1
 * contract (issue #14).
 *
 * Having no paths is the point: every request the frontend might try to make is a type
 * error until the contract exists, which is a great deal better than a client that
 * compiles against shapes somebody guessed.
 */
export type paths = Record<string, never>;
export type webhooks = Record<string, never>;
export type components = Record<string, never>;
export type $defs = Record<string, never>;
export type operations = Record<string, never>;
