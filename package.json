{
	"name": "repolens",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "RepoLens \u2014 deterministic, evidence-backed architecture reports for a public GitHub repository at an exact commit SHA.",
	"license": "UNLICENSED",
	"packageManager": "pnpm@11.20.0",
	"engines": {
		"node": ">=22.12.0"
	},
	"scripts": {
		"check": "pnpm --recursive check",
		"build": "pnpm --recursive build",
		"lint": "pnpm --recursive lint",
		"format": "pnpm --recursive format",
		"test": "pnpm --recursive test"
	}
}
