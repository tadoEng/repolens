// See https://svelte.dev/docs/kit/types#app.d.ts

declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		// interface PageData {}
		// interface PageState {}
		//
		// App.Platform is intentionally left undeclared: this app is served as static
		// assets by Cloudflare and has no server runtime to expose a platform object.
	}
}

export {};
